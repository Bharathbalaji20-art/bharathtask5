# bharathtask5
This project trains a Decision Tree Classifier, visualizes the tree, and analyzes overfitting by controlling tree depth. A Random Forest is also trained and compared for accuracy. Feature importances are interpreted, and both models are evaluated using cross-validation to assess performance.
